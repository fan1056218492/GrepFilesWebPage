{
  "name": "grepfiles-web-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css script.js assets dist/",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name grepfileswebpage --branch main --commit-dirty=false"
  }
}
